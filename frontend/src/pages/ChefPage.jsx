// Import necessary libraries and components for the Chef page
import PizzasList from "../components/PizzasList";

// Define the ChefPage component to manage pizzas or toppings
const ChefPage = () => {
// Render the structure and components of the Chef page
  return (
    <div>
      <h1>Chef Dashboard</h1>
      <PizzasList />
    </div>
  );
};

export default ChefPage;
